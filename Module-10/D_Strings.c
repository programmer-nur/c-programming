#include <stdio.h>
#include <string.h>
int main() {
  char str_one[11];
  char str_two[11];

  scanf("%s", str_one);
  scanf("%s", str_two);

  int length_one = strlen(str_one);
  int length_two = strlen(str_two);

  char concatenating[21];

  return 0;
}